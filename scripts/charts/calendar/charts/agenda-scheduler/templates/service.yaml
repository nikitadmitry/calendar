apiVersion: v1
kind: Service
metadata:
  name: {{ include "agenda-scheduler.fullname" . }}
  labels:
    {{- include "agenda-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agenda-scheduler.selectorLabels" . | nindent 4 }}
